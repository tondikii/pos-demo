# Final Kurasi — Mobile Kasir (Expo)

**Jumlah final: 6 gambar** — rentang 4–8 terpenuhi; capture ulang dengan **tablet medium horizontal (1024×768, deviceScaleFactor 2)** sesuai permintaan, bukan phone portrait (390×844) awal.

> Source awal `portfolio-captures/mobile/` berisi 6 phone-portrait (51–140K) yang terlalu sempit untuk showcase layout tablet (grid kiri + cart panel kanan 34%). Capture awal juga gagal menambah item karena shift belum dibuka (lock state "Belum ada shift") — diperbaiki dengan flow lengkap: tap PIN `123456` → Buka Shift (kas awal 50000) → grid terlihat → tambah 2 produk → keranjang terisi. Landing blank fix juga diterapkan (reducedMotion) meski tidak relevan untuk mobile.
>
> **Update parity Expo Web ↔ Android (tablet medium horizontal 1024×768) — 2 ronde:**
> - **Ronde 1 (sebelumnya):** (1) `app/(auth)/login.tsx:177` — `Platform.OS==='web'` fallback `paddingTop 32px (24+8)` & `paddingBottom 16px` (SafeAreaInsets web=0 vs Android ~24). (2) `src/components/pos/ProductGrid.tsx:265` — `windowWidth-15` untuk scrollbar web & `objectFit:cover` pada `Image` agar kartu tidak stretch. (3) `src/components/ui/Sheet.tsx` + `app/(app)/pos.tsx:125` — `gap` fallback untuk `KeyboardAvoidingView` di web, agar modal Buka Shift rounded-t-3xl identik. (4) `app/(app)/shift.tsx:63` — `RunningShiftCard` `gap-3→gap-4 (12→16px)` + `contentContainerStyle gap:16` mengatasi "kurang gap antar vertical komponen" shift berjalan di web (Animated.View gap tidak full di RN Web).
> - **Ronde 2 (fix spesifik laporan terbaru — sesuai screenshot 00.41.07 web 8081 vs 00.41.45 Android):** (5) **Halaman input PIN styling tidak sama** — `app/(auth)/login.tsx:177,185,207,255,317` web `Left-aligned + single column numpad (1-8 vertikal)` vs Android `Centered header + 3×4 grid`. Penyebab: `Animated.View` + arbitrary `max-w-[348px]/w-[104px]/m-[6px]` & `items-center` class tidak ter-compile di `react-native-web` untuk `Animated.View`, dan `font-extrabold` tidak map ke `PlusJakartaSans`. Fix: header/outlet/dots/numpad semua `Animated.View` diberi `style` fallback web eksplisit (`alignItems:center`, `maxWidth:348, alignSelf:center, flexDirection:row, flexWrap:wrap, justifyContent:center` untuk numpad, `width:104,height:64,margin:6,borderWidth:1,backgroundColor:#FFF,borderRadius:12` untuk tombol, `fontFamily:'PlusJakartaSans_800ExtraBold/700Bold/600SemiBold'` untuk semua `Text`, outer `minHeight:'100vh',backgroundColor:'#F8FAFC'`), plus `rounded-2xl` outlet card `backgroundColor:#FFFFFF,borderRadius:16` — kini di 1024×768 web `1,2,3` di `y=342` sama baris (`x 344,460,576`), header `alignItems:center`, font `PlusJakartaSans_800ExtraBold 800 #0F172A` identik Android (screenshot `01-login.png` 69K→75K, centered). (6) **Modal Buka Shift background transparan** — `src/components/ui/Sheet.tsx:85` overlay `bg-overlay` tidak ada di DOM portal web + `flex-1` tidak `100vh`. Fix: `style={backgroundColor:'rgba(15,23,42,0.5)',position:'absolute',top:0...}` + outer `height:'100vh'` + sheet `backgroundColor:'#FFFFFF',borderRadius:24` — kini dim `rgba(15,23,42,0.5)` terdeteksi dan `02-buka-shift.png` 110K→88K dengan dim benar.
> - **Ronde 3 (fix product card grid 00.49.59 — web single column vs Android 4 kolom):** `src/components/pos/ProductGrid.tsx:271` threshold `768→640` agar split-view kiri ~675px (1024×0.66) tetap 4 kolom seperti Android Medium Tablet (bukan 2), dan `cardWidth` di web dipaksa `max 153px` (bukan 154) karena `Math.floor` dengan sub-pixel 675.844 → 1px over (652+24=676 >675) menyebabkan wrap ke 3 kolom (x `[12,178,344]`); fix `if(web&&numColumns===4) cardWidth=Math.min(cardWidth,153)` + `ScrollView contentContainerStyle {width:'100%'}` & `View px-3` `style {width:'100%',flexDirection:'row',flexWrap:'wrap'}` agar container full width — kini `cardsX [12,177,342,507]` 4 per row, `card 153px` (vs Android 4 per row Espresso/Americano/Cappuccino/Cafe Latte), screenshot `03-kasir-grid.png` 522K→811K & `04-kasir-keranjang.png` 570K→856K identik Android.

## Urutan Naratif (kasir flow <15 detik: auth → buka shift → jualan → rekap)

| # | File | Alasan dipilih (1 kalimat, kaitkan kriteria) |
|---|------|-----------------------------------------------|
| 1 | `01-login.png` | Menceritakan alur dalam 3 detik: outlet pills (Tebet/Blok M/Kemang), PIN dots 6 digit, numpad thumb zone 104×64, lock 5 menit — keamanan kasir tanpa butuh konteks dashboard. |
| 2 | `02-buka-shift.png` | Kompleksitas teknis nyata: Sheet Buka Shift dengan input Rp 50.000 (formatMoneyInput + separator), hint kas awal, tombol Buka Shift — entry operasional sebelum jualan. |
| 3 | `03-kasir-grid.png` | Variasi vs login: tablet landscape grid 13 produk Kopi Senja (Espresso–Kentang Goreng) + foto, badge stok menipis/habis, kategori chips Kopi/Minuman/Makanan/Snack dengan counts, cart panel kanan 34% — layout responsive yang hanya terlihat di tablet. |
| 4 | `04-kasir-keranjang.png` | Paling berbobot (kriteria kompleksitas): keranjang terisi 2 item (Espresso S, Americano M), kalkulasi subtotal + pajak 11% otomatis, pilih metode Cash/QRIS/Transfer, input uang & kembalian, tombol BAYAR — state + kalkulasi, bukan layar kosong. |
| 5 | `05-riwayat.png` | Alur produk standalone: riwayat transaksi dengan badge completed/void, void dengan alasan → stok kembali — audit trail yang dipahami owner tanpa lihat dashboard. |
| 6 | `06-shift.png` | Mewakili keunggulan operasional PRD Flow 3: rekap shift (kas awal, cash masuk, void, expected/actual/selisih ±, breakdown per metode) — keputusan kas harian yang actionable. |

## Dipertimbangkan tapi Tidak Dipilih

- `01-login-tablet.png` asli phone `_tmp_login.png` (68K) & `_tmp_pos_empty.png` (51K) — phone portrait terlalu sempit, cart bar bottom overlay tidak menunjukkan panel kanan tablet; diganti tablet landscape yang lebih menjual untuk listing freelance (klien lihat tablet kasir di meja kasir).
- `02-after-pin-tablet.png` (88K) & `03-pos-lock-tablet.png` (88K) — state transisi "Menunggu redirect" / "Belum ada shift" (kunci): kurang berbobot vs `02-buka-shift` yang menampilkan form input dan `03-kasir-grid` yang sudah berisi produk; tidak standalone.
- `09-pengaturan-tablet.png` (154K) — pengaturan outlet/profil/logout + StatusPill: duplikasi pola list dengan `05-riwayat`/`06-shift`, kurang storytelling transaksi; dipakai jika butuh 7 gambar, saat ini 6 sudah paling representatif.
- `_tmp_history.png` (89K) & `_tmp_settings.png` (140K) phone — diganti tablet horizontal yang konsisten orientasi.
- `_tmp_pos_cart.png` phone (51K) — gagal load grid karena shift lock, keranjang kosong; diganti `04-kasir-keranjang.png` tablet yang terisi nyata.

## Kriteria yang Dipakai

Kompleksitas (keranjang terisi > kosong), storytelling 3 detik (langsung paham ini kasir), variasi (hindari 2 list sama), keunggulan PRD (offline-first badge di header + kalkulasi pajak per outlet), kualitas render (data Kopi Senja wajar, tidak 0 semua), showcase mandiri untuk klien yang hanya butuh app kasir tanpa dashboard.
