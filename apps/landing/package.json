{
  "name": "@larispos/landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 4321",
    "build": "astro build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.astro 2>&1 | head -n 50; echo done",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^7.2.2",
    "astro-robots-txt": "^1.0.0",
    "astro-sitemap": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
